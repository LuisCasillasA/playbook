// Example 5: using Map to convert all elements in a list to lowercase
const names = ['Asabeneh', 'Mathias', 'Elias', 'Brook']
const namesToLowerCase = names.map((name) => name.toLowerCase())

console.log("Example 5: using Map to convert all elements in a list to lowercase")
console.log(namesToLowerCase)
